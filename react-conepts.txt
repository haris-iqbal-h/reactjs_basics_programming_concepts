React-Basic
	Syntax Jsx
	Function declaration expression arrow
	Props 
	Styles internal(object) external(.css) inline(style={{}})
	Events	
	Error handling (try catch)
	spread and rest operator
	destructuring
    props drilling
    context API useContext()
    hooks (useState, useEffect, useContext, useReducer)
	conditional renderin
	import and export	path prefernce(import, require(''))
	list and keys
	forms
	react router (default/error) route 
	assets (images/videos/react-player)
	High Order Components
	React Redux

ReactConcepts

hoisting
props drilling
lifting up state
